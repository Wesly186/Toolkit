spring:
  messages:
    basename: i18n/message
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shiro-learn?useUnicode=true&characterEncoding=utf-8&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: hzsl@1234

mybatis:
  mapper-locations: mapper/*.xml

redis:
  mode: single
  host: localhost
  port: 6379
  database: 0
  password:
  clientName: shiro-learn
  connectionPoolSize: 10
  connectionMinimumIdleSize: 5
  idleConnectionTimeout: 20000
  retryAttempts: 5
  retryInterval: 2000